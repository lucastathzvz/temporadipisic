let myObject = {
  'name': 'base',
  'type': 'example',
  'version': 1.0,
  'active': true
};

// Accessing values
console.log(myObject.name); // Output: base
console.log(myObject['name']); // Output: base
